DROP TABLE FIVE_SUGGEST
CASCADE CONSTRAINTS;

DROP SEQUENCE SEQ_FIVE_SUGGEST;

CREATE TABLE FIVE_SUGGEST(
   SEQ NUMBER(8) PRIMARY KEY,
   ID VARCHAR2(50) NOT NULL,
   REF NUMBER(8) NOT NULL,
   STEP NUMBER(8) NOT NULL,
   DEPTH NUMBER(8) NOT NULL,
   TITLE VARCHAR2(200) NOT NULL,
   CONTENT VARCHAR2(4000) NOT NULL,
   WDATE DATE NOT NULL,
   DEL NUMBER(1) NOT NULL,
   READCOUNT NUMBER(8) NOT NULL
);

CREATE SEQUENCE SEQ_FIVE_SUGGEST
START WITH 1
INCREMENT BY 1;

ALTER TABLE FIVE_SUGGEST
ADD CONSTRAINT FK_FIVE_SUGGEST_ID FOREIGN KEY(ID)
REFERENCES FIVE_MEMBER(ID); 

SELECT *
FROM FIVE_SUGGEST

SELECT SEQ, ID, REF, STEP, DEPTH, TITLE, CONTENT, WDATE, DEL, READCOUNT  
FROM BBS
WHERE ID LIKE '%BBB%'
OR TITLE LIKE '%추가%'
ORDER BY SEQ DESC

DELETE BBS

UPDATE BBS
SET DEL = 0
WHERE ID = 'aaa'


SELECT COUNT(*) FROM FIVE_MEMBER
WHERE ID='eee'